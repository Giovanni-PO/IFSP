# Inventario-IFSP

1# Abra o Mysql  e nele selecione o arquivo db.sql da pasta banco.

2#rode o db.sql no mysql.

3# Abra o VScode(IDE da maquina) abra o primeiro terminal cmd e rode a seguinte ordem de código. OBS: o VScode por padrão não abre o terminal cmd, no canto superior direito do terminal a o seu modelo clique na seta para baixo do lado do tipo de terminal e coloque para cmd
  1 - cd frontend
  2 - npm install -y
  2 - npm run dev

4# Abra um segundo terminal cmd no VScode e rode os seguintes codigos. OBS: o VScode por padrão não abre o terminal cmd, no canto superior direito do terminal a o seu modelo clique na seta para baixo do lado do tipo de terminal e coloque para cmd
  1 - cd backend
  2 - node server.js

#5 no terminal do frontend entre no link que aparece depois de executar os codigos do passo 3, 4 e rodar o banco no Mysql.
